# Build and Development Tools
setuptools>=58.0
pytest>=7.0

# Configuration
PyYAML>=6.0

# Note: ROS 2 packages (rclpy, std_msgs, launch, launch_ros) 
# are typically installed via: sudo apt install ros-<distro>-*
# or already available in your ROS 2 environment
